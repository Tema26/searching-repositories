export default {
   nameRepres(state) {
      return state.searchNickname;
   },
   response(state) {
      return state.arrayOfRepositoresDetails;
   },   
   returnPage(state) {
      return state.page;
   }
}